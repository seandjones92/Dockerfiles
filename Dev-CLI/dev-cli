
if podman container ls -a | grep dev-cli >/dev/null
then
    podman start -i -a dev-cli
else
    podman run -it --privileged=true --name dev-cli -v /home/$USER/:/root -v /tmp/.X11-unix/:/tmp/.X11-unix -e DISPLAY=:0 dev-cli
fi
